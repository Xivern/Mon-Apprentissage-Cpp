#ifndef MODIFTABLEAU_HPP_INCLUDED
#define MODIFTABLEAU_HPP_INCLUDED

/*
Un fichier d'en-tête dont l'extension est .hpp  : il contient uniquement la description de la fonction, ce qu'on appelle le prototype de la fonction.
*/

/*
 * Fonction qui permet la mise à 0 de toutes les cases d'un tableau
 * - tableau : tableau de variable qui sera affecté par la mise à 0
 * - taille : nombre de case du tableau
 * Valeur retournée : NONE
 */
void intRAZTableau(int tableau[], int taille);

#endif // MODIFTABLEAU_HPP_INCLUDED